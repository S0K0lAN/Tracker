# Расширенные функции Focus Flow

## Статус

- Версия браузерного MVP: schema v4.
- Дата актуализации: 21 августа 2026 года.
- Область документа: реализованные пользовательские сценарии, хранение, ограничения и проверки.
- Подробная трассировка критериев: [feature-acceptance.md](feature-acceptance.md).

Focus Flow остаётся персональным local-first приложением. Все описанные ниже
изменения сначала применяются локально и не ждут сеть. Браузерная версия
проверяет продуктовые сценарии, но не заменяет будущую Tauri-сборку и live
OAuth-проверку Google Drive.

## Навигация и рабочие страницы

| Маршрут | Назначение |
|---|---|
| `/today` | Просроченные задачи, запланированные на сегодня и дедлайны сегодня |
| `/inbox` | По умолчанию неразобранные задачи без проекта, начала и дедлайна; фильтр «Все» возвращает общий список; list/board без calendar shortcut |
| `/projects` | Создание проектов, обзор и задачи выбранного проекта |
| `/calendar` | Год, месяц, неделя, три дня, день и диаграмма дедлайнов |
| `/matrix` | Четыре квадранта важности и вычисленной срочности |
| `/search` | Поиск по задачам, проектам, тегам и сохранённым фильтрам |
| `/habits` | Привычки, индивидуальный ритм, прогресс и серии |
| `/trash` | Корзина и архив завершённых задач |
| `/settings` | Оформление и типографика, поведение, синхронизация, плагины и локальные данные |

Неизвестный URL показывает контролируемую страницу 404. Прямые ссылки,
Back/Forward и reload поддерживаются History API router.

## Задачи и редактор

Название остаётся единственным обязательным полем. Полный редактор поддерживает:

- дополнительный текст, проект, подзадачи и теги;
- плановое начало и единый дедлайн;
- низкую или высокую важность;
- автоматическую срочность по дедлайну, индивидуальный порог и ручное
  переопределение;
- до пяти напоминаний;
- до пяти файлов или изображений размером до 1 МБ каждый;
- вставку изображения из буфера и drag-and-drop файлов;
- запуск привязанного к задаче Помодоро.

Нажатие на существующую задачу сначала открывает отдельный читаемый просмотр:
описание, даты, важность, срочность, подзадачи, теги, напоминания и вложения.
Из панели просмотра доступны таймер, завершение/возврат, редактирование,
архивация и корзина; редактор открывается только по явной кнопке.

Проект, важность, порог срочности и ручная срочность выбираются через общий
combobox. Он показывает выбранное состояние, пояснения и цвет проекта.
Проектный список поддерживает поиск. Доступны мышь/touch, `Enter`, `Space`,
стрелки, `Home`, `End` и `Escape`; `Escape` закрывает только открытый список и
возвращает фокус на trigger, не уничтожая черновик редактора.

На карточке важность обозначена и иконкой, и текстом «Обычно»/«Важно», поэтому
смысл не зависит только от цвета. Также видны срочность, проект, дедлайн,
подзадачи, вложения, теги и накопленные минуты фокуса.

### Вложения

Из редактора можно открыть изображение, PDF или текстовый файл в отдельном
viewer, скачать его и масштабировать изображение. Viewer закрывается кнопкой
или `Escape`, после чего фокус возвращается к источнику.

В браузерном MVP бинарные данные хранятся как data URL внутри локального
snapshot. Лимиты защищают `localStorage` от неконтролируемого роста. Для
desktop/Android реализации они должны быть перенесены в отдельный Blob/File
store, а в JSON должны остаться метаданные и content hash.

### Голосовой ввод

Кнопка «Надиктовать задачу» использует доступный в браузере Web Speech API с
локалью `ru-RU`. Запись не сохраняется приложением. При неподдерживаемом API
остаётся текстовый fallback, поэтому ручное создание не блокируется.

Детерминированный parser выделяет:

- название;
- `сегодня`, `завтра`, `послезавтра` или следующий названный день недели;
- время после «в», «к» или «до»;
- высокую важность;
- `#теги`;
- подсказку проекта.

Дата и время без маркера записываются в `startAt`, а `deadline` очищается.
Маркеры «до» перед датой/временем, «дедлайн» и «срок» направляют распознанное
значение в `deadline` и очищают `startAt`. Preview явно подписывает «Начало»
или «Дедлайн»; применение также сбрасывает устаревшую ошибку ручного ввода даты.

Результат сначала появляется в preview. Пользователь явно применяет или
отменяет распознанные поля и после применения может исправить любое поле
обычным редактором. Распознавание речи браузером может использовать сервис
поставщика браузера; Focus Flow сам не отправляет и не хранит аудиозапись.

## Представления задач

### Сегодня

Страница использует локальную календарную дату. Она разделяет просроченные
активные задачи, задачи с `startAt` сегодня и задачи с дедлайном сегодня.
Задача, у которой и начало, и дедлайн сегодня, не дублируется.

### Проекты

Проект создаётся и редактируется с названием, описанием и цветом. Пустое имя
не принимается. Карточка проекта показывает количество активных задач и
ближайший срок, а меню позволяет изменить или удалить проект с повторным
подтверждением. При удалении задачи проекта переводятся в системный проект
«Без проекта», а ссылки сохранённых фильтров очищаются. Датированная задача при
этом не попадает в default-режим «Неразобранные», но остаётся доступной через
«Все». После открытия проекта отображается его
собственный список; новый проект сразу появляется в searchable selector
редактора задачи. Изменения задач и проектов сохраняются после reload.

### Входящие

«Входящие» открываются на задачах системного проекта `inbox` без `startAt` и
`deadline`. Быстрый фильтр «Все» возвращает прежний общий набор, включая
проектные и датированные задачи; корзина и архив всегда исключены. Доступны:

- сортировка по дате создания, ближайшему дедлайну, важности или названию;
- фильтр тегов и быстрые фильтры «Неразобранные», «Все», «Сегодня», важности и срочности;
- режимы «Список» и «Доска»;
- календарь как самостоятельный маршрут общей навигации `/calendar`, без
  отдельного shortcut из Входящих;
- показ завершённых и массовая архивация завершённых задач только выбранного режима.

Доска показывает тот же отфильтрованный набор. Отдельный календарь
использует `startAt` для временной сетки, а deadline-only задачи показывает
markers и диапазонная диаграмма; дедлайн не подменяет плановое начало.
Выбранные вид Входящих и сортировка сохраняются в настройках. Историческое
значение `inboxView=calendar` при миграции schema v2 преобразуется в `list`.

### Поиск и сохранённые фильтры

Глобальный поиск одновременно находит:

- задачи по названию, тексту и тегам;
- проекты по названию и описанию;
- теги;
- сохранённые фильтры.

Визуальный конструктор фильтра поддерживает проект, несколько тегов с
семантикой «любой/все», важность, вычисленную срочность и статус. Фильтр можно
именовать, сохранить, повторно применить и удалить. Поиск и фильтрация не
изменяют исходные задачи.

### Календарь и дедлайны

Задача с `startAt` остаётся календарным событием. Задача только с дедлайном
теперь дополнительно видна как deadline marker в недельном и месячном режимах.
Отдельная диаграмма дедлайнов показывает все задачи с дедлайном в порядке
срока. Поэтому дедлайн не превращается в плановое начало и не меняет
семантику time blocking.

### Жизненный цикл, корзина и архив

Статусы задачи: `active`, `completed`, `archived`, `deleted`.

- Завершение обратимо и не удаляет данные.
- «В корзину» выполняет мягкое удаление и запоминает предыдущий статус.
- Из корзины задачу можно восстановить или удалить окончательно после
  подтверждения.
- Завершённую задачу можно архивировать отдельно либо массово.
- Архив отделён от корзины и поддерживает восстановление.

## Помодоро

Меню активной задачи запускает глобальный dock-таймер с циклами:

- фокус — 25 минут;
- короткий перерыв — 5 минут;
- длинный перерыв — 15 минут после четырёх фокус-сессий.

Доступны старт, пауза и сброс. Текущая задача, режим, остаток времени,
`runningSince` и число завершённых сессий сохраняются. После полного
фокус-цикла к задаче добавляются минуты фокуса. Браузерный MVP не обещает
системный таймер при полностью закрытом браузере.

## Привычки

Привычка содержит название, необязательное описание, цвет, расписание по дням
недели, собственные completions и одну из десяти доступно подписанных иконок.
Существующую привычку можно отредактировать без потери её истории выполнения,
но текущая форма меняет только название, описание и иконку. Новая привычка
создаётся ежедневной; пользовательская настройка `targetDays` и цвета пока
отсутствует.

«Ваш ритм» вычисляется для каждой привычки отдельно на скользящем окне семи
локальных дней:

- в знаменатель входят только запланированные для этой привычки дни;
- прогресс и серия используют только её собственные completions;
- незапланированные дни визуально отличаются и не разрывают серию;
- переключение одной привычки не меняет другую.

## Оформление, шрифт и фоны

Кроме светлой/тёмной/системной темы, акцента и плотности доступны:

- три безопасных системных стека шрифтов без сетевой загрузки;
- глобальный масштаб текста 90%, 100%, 110% или 120%;
- «Без фона»;
- встроенные пресеты «Туман», «Рассвет» и «Лес»;
- собственное локальное изображение до 1,5 МБ;
- регулируемое затемнение фона для сохранения читаемости.

Выбор применяется ко всему пространству и сохраняется. Собственный фон в
браузерном MVP хранится как data URL в snapshot; будущий platform adapter
должен вынести его в отдельное файловое хранилище.

## Удалённое хранилище и OAuth

Синхронизация остаётся local-first: изменение сразу сохраняется локально и не
ждёт сеть. Реестр `SyncProviderRegistry` подключает провайдер через descriptor,
публичные config-поля и runtime, который сам владеет авторизацией и эфемерной
сессией. Поэтому второй implicit или OAuth-провайдер не требует Google-веток в
`AppContext`, модели задач или странице настроек.

Каждое сохраняемое config-поле descriptor обязано иметь явную классификацию
`persistence: public`. Secret-поля и credential-подобные имена registry
отклоняет: access/refresh token должен жить только внутри authorization/runtime
и будущего защищённого secret store, а не в универсальной конфигурации.

Google Drive runtime использует Google Identity Services, scope
`drive.appdata` и файл `focus-flow-data.json` в `appDataFolder`. Реализованы:

- подключение и повторный вход после истечения/отклонения access token;
- чистая авторизация без автоматического чтения или записи данных при входе;
- создание, поиск, скачивание и обновление snapshot;
- отдельное «Получить» только в направлении remote → local: отсутствие файла
  ничего не меняет, а отличающаяся копия проходит preview, подтверждение и
  создание rollback backup до применения;
- отдельное «Отправить» только в направлении local → remote: remote не
  применяется локально, запись защищена проверкой revision, а отличающаяся
  копия требует явного подтверждения;
- умное «Синхронизировать» и debounce-автосинхронизация через общий reconcile,
  который выбирает направление по remote metadata и общей базе;
- сравнение remote ID, revision и hash общей базы;
- явный конфликт без автоматической перезаписи и отдельный backup до импорта;
- восстановление копии до последнего импорта из интерфейса;
- типизированные offline, auth, forbidden, conflict и rate-limit ошибки.

Browser token flow не выдаёт приложению постоянный refresh token. Выбранный
провайдер, публичная конфигурация и remote metadata переживают reload, а access
token хранится только в памяти; после перезапуска нужен пользовательский клик
«Продолжить». Полностью фоновое постоянное подключение требует BFF либо Tauri
с Authorization Code + PKCE и системным secret store.

Drive v3 возвращает version, но документированный `files.update` не обещает
атомарный compare-and-swap для media upload. Адаптер повторно проверяет revision
непосредственно перед PATCH, однако это best-effort защита с остаточным окном
гонки. Трёхсторонний entity merge, tombstones и серверно-атомарный протокол
остаются следующим этапом.

## Расширения

`PluginRegistry` сейчас является минимальным in-process каркасом: он умеет
зарегистрировать объект, вызвать `activate/deactivate` и отфильтровать записи
по slot. Это не готовый Plugin API v1. В текущем срезе отсутствуют permissions,
sandbox, проверка совместимости версии, namespaced storage и безопасная модель
выполнения стороннего JavaScript. Подпись «API v1» на странице настроек следует
считать направлением развития, а не production-гарантией.

## Данные и миграция

Текущая версия snapshot — `4`. При загрузке `normalizeAppState`:

- добавляет новые обязательные поля старым задачам, проектам и привычкам;
- нормализует статусы и стандартный порог 72 часа;
- добавляет `savedFilters`, `pomodoro` и новые настройки;
- сохраняет неизвестные поля верхнего уровня и сущностей через структурное
  расширение, чтобы не уничтожить будущие plugin-данные.

`LocalStorageAdapter` до нормализации проверяет структуру, известные скаляры,
уникальность идентификаторов, canonical inbox, ссылки между сущностями,
суммарный объём строк, число узлов и глубину неизвестных plugin-данных.
Исторические schema v1–v3 детерминированно получают безопасные уникальные ID и
исправленные ссылки без удаления сущностей; типографика получает безопасные
значения `system`/`100`, отсутствующие provider metadata и прежний
`inboxView=calendar` не считаются повреждением. Адаптер не
понижает и не перезаписывает snapshot более новой версии. Перед каждой записью
предыдущий основной snapshot копируется в `focus-flow.state.v1.backup`.
Повреждённый основной snapshot восстанавливается из backup, если он валиден.
Перед импортом remote дополнительно создаётся `import-backup`, доступный для
восстановления на странице настроек.
Это recovery-механизм браузерного прототипа, а не эквивалент атомарного
файлового адаптера Tauri.

Все записи проходят через инъецируемый `StorageAdapter` и одну сериализованную
очередь. Ожидающие обычные snapshots coalesce до последней ревизии, а
import/reset/remote replacement служат явными барьерами: запоздалая старая
запись не может завершиться после новой и вернуть удалённые данные.

Редактор задачи отдельно сохраняет ограниченный аварийный journal с debounce и
ключом create/task. При следующем открытии пользователь явно выбирает
восстановление или удаление; journal очищается только после подтверждённой
локальной записи либо осознанного discard. Вложения в journal не входят и
остаются задачей будущего BlobStore.

Тот же версионированный envelope доступен как переносимая JSON-копия в
«Настройки → Локальные данные». Экспорт исключает OAuth-сессию и device-local
настройки провайдера. Импорт ограничен 10 МБ, валидируется до изменения state,
показывает сводку и требует явного подтверждения. Перед заменой создаётся
`import-backup`; ошибка чтения, схемы или записи не заменяет основной snapshot.

OAuth-токен не попадает в snapshot, localStorage или Drive-файл. Публичные
настройки провайдеров хранятся в отдельном device-local `syncProviderConfigs`,
который целиком исключён из remote envelope и sync hash.

Вложения и пользовательский фон принимают только ограниченные по размеру
base64 `data:`-URL с проверяемым MIME; внешние URL, `javascript:` и
несовпадающий MIME отклоняются до импорта.
Viewer повторяет проверку, а текстовый/PDF iframe запускается в `sandbox`.

## UI/UX, доступность и производительность

Интерфейс использует общие цветовые токены, размеры и Lucide-иконки.
Интерактивные SVG центрируются внутри button/nav primitives. Touch targets
checkbox задачи, выбора цвета проекта, иконок и дней привычки имеют 44×44 px
без визуального утяжеления внутренних маркеров.

Responsive/layer регрессии из аудита 10 августа 2026 года закрыты: React и CSS
используют единый Sidebar breakpoint 820 px, а portal `SelectMenu` расположен
выше modal backdrop. Повторный browser gate включает 1024×900, pointer и
keyboard-сценарии dropdown внутри редактора.

Просмотр и редактор задачи, viewer вложения и мобильный drawer удерживают клавиатурный
фокус внутри активного modal scope, поддерживают `Escape` и возвращают фокус
на исходный control. Фоновый интерфейс на время открытия получает `inert`.

Анимации используют отдельную easing-переменную: значение transition больше
не интерпретируется браузером как скрытая animation delay. Page/editor
анимации короткие и не оставляют постоянный transform stacking context.
`prefers-reduced-motion` и пользовательская настройка отключают движение.
Смена маршрута сбрасывает scroll мгновенно, поэтому fixed mobile header не
мерцает во время compositor smooth-scroll.

Для длинного списка карточки используют `content-visibility` и containment,
который временно снимается у открытого action menu. Начиная со 150 задач
Входящие включают windowed list: полная высота прокрутки сохраняется, а в DOM
остаются видимые карточки с небольшим запасом. Performance gate создаёт 500
задач и проверяет первую отрисовку, p95 кадра и долю длинных кадров.

## Автоматические проверки

Обязательная передача выполняется командами:

```bash
npm test
npm run build
npm run test:e2e
```

`npm run check` выполняет только `npm test` и `npm run build`; E2E в него не
входит. Playwright-конфигурация требует установленный Google Chrome через
`channel: "chrome"`; `npm ci` браузер не устанавливает. Альтернативный
Playwright Chromium потребует явного изменения конфигурации и нового полного
gate.

На Node.js 26 Vitest/jsdom конфликтует с экспериментальным глобальным Web
Storage Node. Проверенный временный локальный запуск:

```bash
NODE_OPTIONS=--no-experimental-webstorage npm test
```

Исторический состав набора на 11 августа — 25 Vitest-файлов/184 теста и 4
Playwright-файла/34 сценария. При повторной проверке 11 августа 2026 года с указанным флагом
unit/component прошли 184/184, production build прошёл. Попытка E2E 10 августа
не дошла до browser assertions: системный Chrome отсутствовал. В репозитории
нет CI; это датированный локальный снимок, а не непрерывная гарантия.

Основное evidence:

- `src/domain/models.test.ts` — срочность и просрочка;
- `src/domain/voiceParser.test.ts` — русский parser и границы слов;
- `src/components/TaskEditor.test.tsx` — применение voice preview, очистка
  альтернативной даты и восстановление валидности;
- `src/components/TaskDetails.test.tsx` — режим чтения и действия задачи;
- `src/pages/SettingsTypography.test.tsx` — мигрируемая глобальная типографика;
- `src/pages/HabitsPage.test.tsx` — ритм, независимые completions, иконки и
  описание;
- `src/NewFeatures.test.tsx` — страницы, проекты, lifecycle, layouts,
  сортировка, keyboard Escape и deadline markers;
- `src/core/sync/GoogleDriveAdapter.test.ts` — контракт Drive API;
- `e2e/app.spec.ts` — базовый task loop и старые рабочие представления;
- `e2e/new-features.spec.ts` — новые маршруты, persistence, поиск, фильтры,
  вложения, голосовой fallback, Pomodoro, фоны и привычки;
- `e2e/task-details.spec.ts` — просмотр задачи и наблюдаемые действия toolbar/menu;
- `e2e/ui-quality.spec.ts` — WCAG AA/overflow и прокрутка 500 задач.

Независимый visual QA дополнительно проверяет обе темы на 1440×900 и 390×844,
keyboard/focus, геометрию dropdown, contrast и stacking. Последний записанный
полный visual/browser gate до аудита относится к 3 августа 2026 года; ниже
фиксируются более новые датированные результаты, включая 1024 px.

Полный локальный gate 21 августа 2026 года на Node.js 18.19.1:

- `npm test`: 35 файлов, **376/376 unit/component-тестов**;
- `npm run build`: TypeScript и production Vite build — успешно;
- `npm run test:e2e`: 5 файлов, **40/40 Playwright-сценариев**;
- `npm audit` и `npm audit --omit=dev`: 0 известных уязвимостей;
- 500 задач: render 552 мс, p95 frame 23,0 мс, long frames 0%;
- light/dark WCAG AA, horizontal overflow, browser errors, 1440×900,
  1024×900 и 390×844 прошли.

Это датированный локальный результат; непрерывного CI в репозитории нет.

## Продуктовые ориентиры

Из Todoist использованы принципы быстрого захвата, отдельного Today,
progressive disclosure, сохранённых фильтров, нескольких видов и ясной
важности. Из SingularityApp — сочетание проектов, календаря, привычек,
Помодоро, архива, голосового capture и оформления. Корпоративная сложность,
совместная работа и marketplace не переносились в персональный MVP.

Официальные источники для повторной сверки:

- [Todoist: фильтры](https://www.todoist.com/help/articles/introduction-to-filters-V98wIH)
- [Todoist: напоминания](https://www.todoist.com/help/articles/introduction-to-reminders-9PezfU)
- [SingularityApp: возможности](https://singularity-app.ru/features/)
- [Google Drive: appDataFolder](https://developers.google.com/workspace/drive/api/guides/appdata)
- [Google Drive: OAuth scopes](https://developers.google.com/workspace/drive/api/guides/api-specific-auth)

## Честные границы браузерного MVP

- Нет Tauri shell, приватного JSON-файла, системного secret store и
  платформенных уведомлений.
- Google Drive OAuth/скачивание/автосинхронизация проверены mock E2E и
  контрактными тестами, но live OAuth smoke с реальным аккаунтом не выполнен.
- Browser-only подключение требует пользовательского продолжения после reload;
  Drive media update имеет документированное выше best-effort окно гонки.
- Вложения и custom background временно занимают localStorage.
- Нет date-only модели без UTC-конвертации, повторяющихся задач, импорта из
  сторонних таск-трекеров, иерархии/архива проектов, drag-and-drop между
  board-колонками и фоновой работы Помодоро при закрытом приложении.
- Plugin API v1 пока не реализован: есть только минимальный локальный registry.
- Web Speech зависит от браузера и его политики обработки речи.
